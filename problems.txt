Встреченные проблема: 
    подключение бд: 
        искало не в том месте:
            решение - явно указать таблицу в models.py

    подключение fontawesome

дрочное создание транзакций(3 часа гугла) 
переопределение views.py под транзакции + прокси

добавление generic для создания видов(ListView/CreateView)

не видело файлы static(в консоли) + после добавления транзакций и alias не видело темплейты (надрачивал настройки)

убрать тех. поля (created_at)
редактирование операций

